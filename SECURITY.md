# Security policy

Please report vulnerabilities privately through GitHub security advisories for
`changl/agent-annotate` rather than opening a public issue.

Agent Annotate binds locally by default. Do not expose a listener publicly
without an authenticated TLS reverse proxy. Never commit Cloudflare tokens,
Codex credentials, provider session transcripts, comments from private
projects, or runtime ownership files.

